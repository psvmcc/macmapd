name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read

  release:
    needs: verify
    name: Publish release image and artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Check out source and history
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Verify tag points to main
        shell: bash
        run: |
          git fetch origin main --force
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Release tags must point to a commit reachable from main" >&2
            exit 1
          fi

      - name: Verify release version
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: python3 scripts/check-release-version.py "$RELEASE_TAG"

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Build cargo-dist release artifacts
        env:
          CONTAINER_ENGINE: docker
          RELEASE_TAG: ${{ github.ref_name }}
        run: just dist-build

      - name: Set image name and version
        id: image
        shell: bash
        run: |
          echo "name=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/macmapd" >> "$GITHUB_OUTPUT"
          echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=raw,value=stable
            type=raw,value=${{ github.ref_name }}
            type=raw,value=${{ steps.image.outputs.version }}
          labels: |
            org.opencontainers.image.version=${{ steps.image.outputs.version }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build release image locally
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          load: true
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke-test release image
        env:
          RELEASE_IMAGE: ${{ steps.image.outputs.name }}:${{ github.ref_name }}
        run: just docker-smoke "$RELEASE_IMAGE"

      - name: Verify release files exist
        run: |
          test -s target/distrib/macmapd-x86_64-unknown-linux-gnu.tar.xz
          test -s target/distrib/macmapd-x86_64-unknown-linux-gnu.tar.xz.sha256

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push tested release image
        env:
          IMAGE_TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          while IFS= read -r tag; do
            test -z "$tag" || docker push "$tag"
          done <<< "$IMAGE_TAGS"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: macmapd ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            target/distrib/*.tar.xz
            target/distrib/*.sha256
